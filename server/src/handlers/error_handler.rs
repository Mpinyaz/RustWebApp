use crate::models::errors::ApiError;
// use axum::http::StatusCode;
// use axum::response::IntoResponse;
// use std::error::Error;

pub async fn handling_errors() -> Result<(), ApiError> {
    Ok(())
}
